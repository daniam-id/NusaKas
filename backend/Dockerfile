FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm ci --only=production

# Copy source
COPY . .

# Build TypeScript
RUN npm run build

# Create wa-session directory
RUN mkdir -p .wa-session

EXPOSE 3000

CMD ["node", "dist/index.js"]
